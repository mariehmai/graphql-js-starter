type Query {
  info: String!
  event: [Event!]!
}

type Mutation {
  post(name: String!, description: String, location: String!): Event!
}

type Event {
  id: ID!
  name: String!
  description: String
  location: String!
}
